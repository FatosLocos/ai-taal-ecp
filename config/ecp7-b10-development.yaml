extends: ecp7-b9-development.yaml

experiment:
  title: ECP-7 position-aware decoder with extended optimization
  arm: position_aware_decoder_extended_optimization
  objective: >-
    Test whether the strongest weak-structure design was underoptimized by
    preserving its original temperature annealing while extending only the
    population optimization horizon and checkpoint-selection window.

training:
  max_steps: 15000
  minimum_steps: 5000
  patience_steps: 3000
  temperature_schedule_steps: 5000
