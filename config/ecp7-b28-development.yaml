extends: ecp7-b25-development.yaml

experiment:
  title: ECP-7 identity-preserving residual parallel sender
  arm: identity_preserving_residual_parallel_sender
  objective: >-
    Test whether one zero-initialized shared residual interaction layer can
    break the stable sender code-capacity ceiling while preserving the
    successful shallow joint-context path and all meaning-free constraints.

agents:
  sender:
    family: residual_bounded_parallel_sender
